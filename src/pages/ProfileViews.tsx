import { FormEvent, useState } from 'react';
import { AppFrame } from '../components/PageFrame';

export function ProfileView({ kind }: { kind: 'farmer' | 'company' }) {
  const farmer = kind === 'farmer';
  const [saved, setSaved] = useState(false);
  const submit = (event: FormEvent) => { event.preventDefault(); setSaved(true); };
  return <AppFrame active="Perfil" role={farmer ? 'agricultor' : 'empresa'} footer><main className="page-container"><div className="container"><div className="content-heading"><div><span className="eyebrow">Tu identidad en la red</span><h1>{farmer ? 'Perfil de agricultor' : 'Perfil de empresa'}</h1><p>Actualiza la información que verán tus contactos comerciales.</p></div><span className="status-pill success">Perfil verificado</span></div>{saved && <div className="page-notice success" style={{ marginBottom: 18 }}>Los cambios se guardaron en la vista de demostración.</div>}<div className="profile-grid"><aside className="surface profile-summary"><span className="avatar">{farmer ? 'CM' : 'AE'}</span><h2>{farmer ? 'Carlos Mendoza' : 'AgroExport S.A.S.'}</h2><p>{farmer ? 'Agricultor · Huila' : 'Empresa compradora · Bogotá'}</p><span className="status-pill success">Activo</span><div className="profile-metrics"><div><strong>{farmer ? '12' : '28'}</strong><span>{farmer ? 'Publicaciones' : 'Proveedores'}</span></div><div><strong>{farmer ? '4.9' : '14'}</strong><span>{farmer ? 'Calificación' : 'Órdenes'}</span></div><div><strong>2026</strong><span>Desde</span></div></div></aside><section><form className="surface" onSubmit={submit}><div className="profile-section-title"><h2>Información general</h2><button className="btn-soft btn-sm" type="button">Cambiar foto</button></div><div className="form-grid" style={{ marginTop: 20 }}><label className="form-field"><span>{farmer ? 'Nombre completo' : 'Nombre de la empresa'}</span><input defaultValue={farmer ? 'Carlos Mendoza' : 'AgroExport S.A.S.'} /></label><label className="form-field"><span>Correo electrónico</span><input type="email" defaultValue={farmer ? 'carlos@fincaelporvenir.co' : 'compras@agroexport.co'} /></label><label className="form-field"><span>Departamento</span><select defaultValue={farmer ? 'Huila' : 'Cundinamarca'}><option>Huila</option><option>Antioquia</option><option>Cundinamarca</option><option>Bogotá</option></select></label><label className="form-field"><span>Municipio</span><input defaultValue={farmer ? 'Pitalito' : 'Bogotá'} /></label><label className="form-field full"><span>Descripción</span><textarea rows={4} defaultValue={farmer ? 'Productor de café especial de altura, comprometido con la calidad y el comercio justo.' : 'Compañía dedicada a la compra y distribución de productos agrícolas colombianos.'} /></label></div><div className="form-actions"><button className="btn-primary" type="submit">Guardar cambios →</button></div></form><div className="surface"><h2>Preferencias</h2><label className="preference-row"><span><strong>Notificaciones de mensajes</strong><small>Recibe alertas de nuevas conversaciones.</small></span><input type="checkbox" defaultChecked /></label><label className="preference-row"><span><strong>Perfil visible en búsquedas</strong><small>Permite que otros usuarios encuentren tu perfil.</small></span><input type="checkbox" defaultChecked /></label></div></section></div></div></main></AppFrame>;
}
