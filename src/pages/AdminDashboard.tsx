import { DashboardFrame } from '../components/PageFrame';
import { DashboardStats } from '../components/Utilities';
import { dashboardByRole, users } from '../data';

export default function AdminDashboard() {
  const data = dashboardByRole.admin;
  return <DashboardFrame role="admin" active="Resumen"><div className="content-heading"><div><span className="eyebrow">{data.eyebrow}</span><h1>{data.title}</h1><p>{data.description}</p></div><span className="date-chip">15 agosto 2026 · 09:45</span></div><DashboardStats stats={data.stats} /><div className="dashboard-columns"><section className="surface"><div className="surface-heading"><h2>Actividad comercial</h2><select className="input-control compact-select"><option>Últimos 6 meses</option><option>Este año</option></select></div><div className="chart-legend"><span><i className="legend-dot green" />Órdenes</span><span><i className="legend-dot orange" />Usuarios nuevos</span></div><div className="chart-placeholder"><div className="chart-line" /><div className="chart-labels"><span>Mar</span><span>Abr</span><span>May</span><span>Jun</span><span>Jul</span><span>Ago</span></div></div></section><section className="surface"><div className="surface-heading"><h2>Usuarios recientes</h2><a className="text-link" href="/admin-users">Ver todos →</a></div><div className="user-list">{users.slice(0, 4).map((user) => <div className="user-row" key={user.id}><span className="avatar">{user.initials}</span><div><strong>{user.name}</strong><small>{user.role} · {user.location}</small></div><span className={`status-pill ${user.status === 'Activo' ? 'success' : user.status === 'Pendiente' ? 'warning' : 'neutral'}`}>{user.status}</span></div>)}</div></section></div><section className="surface" id="reportes"><div className="surface-heading"><h2>Acciones rápidas</h2></div><div className="quick-grid"><a className="quick-action" href="/admin-users"><span>👥</span><div><strong>Gestionar usuarios</strong><small>Revisar altas y permisos</small></div></a><a className="quick-action" href="/admin-publications"><span>📦</span><div><strong>Moderar publicaciones</strong><small>6 pendientes de revisión</small></div></a><a className="quick-action" href="/admin-dashboard#reportes"><span>📈</span><div><strong>Ver reportes</strong><small>Actividad y crecimiento</small></div></a></div></section></DashboardFrame>;
}
