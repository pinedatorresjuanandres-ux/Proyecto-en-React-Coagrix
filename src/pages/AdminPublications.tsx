import { useState } from 'react';
import { DashboardFrame } from '../components/PageFrame';
import { publications } from '../data';

export default function AdminPublications() {
  const [filter, setFilter] = useState('Todas');
  return <DashboardFrame role="admin" active="Publicaciones"><div className="content-heading"><div><span className="eyebrow">Control de calidad</span><h1>Publicaciones</h1><p>Revisa el contenido publicado por agricultores y empresas.</p></div><span className="status-pill warning">6 pendientes de revisión</span></div><div className="surface"><div className="table-toolbar"><div className="tabs"><button className={filter === 'Todas' ? 'selected' : ''} onClick={() => setFilter('Todas')}>Todas <small>386</small></button><button className={filter === 'Pendientes' ? 'selected' : ''} onClick={() => setFilter('Pendientes')}>Pendientes <small>6</small></button><button className={filter === 'Reportadas' ? 'selected' : ''} onClick={() => setFilter('Reportadas')}>Reportadas <small>2</small></button></div><select className="input-control compact-select"><option>Más recientes</option><option>Más vistas</option></select></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Publicación</th><th>Autor</th><th>Precio</th><th>Vistas</th><th>Estado</th><th /></tr></thead><tbody>{publications.slice(0, 6).map((publication, index) => <tr key={publication.id}><td><div className="table-product"><span style={{ background: publication.color }}>{publication.emoji}</span><div><strong>{publication.name}</strong><small>{publication.category} · {publication.location}</small></div></div></td><td>{publication.farmer}</td><td>{publication.price.toLocaleString('es-CO')} / {publication.unit}</td><td>{publication.views}</td><td><span className={`status-pill ${index === 1 ? 'warning' : 'success'}`}>{index === 1 ? 'Pendiente' : 'Activa'}</span></td><td><a href={`/product/${publication.id}`} className="icon-action">→</a></td></tr>)}</tbody></table></div></div></DashboardFrame>;
}
